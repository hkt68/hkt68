import Layout from '@/components/Layout';

function PriceCheckPage({ user, onLogout }) {
  return (
    <Layout user={user} onLogout={onLogout}>
      <h2>Ürün Fiyat Gör</h2>
      <p>Bu sayfa yakında aktif olacak...</p>
    </Layout>
  );
}

export default PriceCheckPage;
